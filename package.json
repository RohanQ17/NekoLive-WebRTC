{
  "name": "nekolive-webrtc",
  "private": true,
  "version": "1.0.0",
  "description": "WebRTC video chat (static) with Edge WebSocket signaling on Vercel",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp main.html room.html streams.js public/ && cp -r styles images public/",
    "vercel-build": "npm run build"
  }
}
